# Copy into each organization's private configuration repository.
# source_allowed stays false until the organization records an endpoint privacy decision.

[models."openrouter/moonshotai/kimi-k2.7-code"]
source_allowed = false
zdr = "pending"
data_collection = "pending"
roles = ["code"]

[models."openrouter/z-ai/glm-5.2"]
source_allowed = false
zdr = "pending"
data_collection = "pending"
roles = ["financial", "business"]

[models."openrouter/google/gemini-3.6-flash"]
source_allowed = false
zdr = "pending"
data_collection = "pending"
roles = ["distributed-systems"]

[models."openrouter/openai/gpt-5.6-terra"]
source_allowed = false
zdr = "pending"
data_collection = "pending"
roles = ["openai", "code"]
